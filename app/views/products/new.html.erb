<h1>Product Resistration</h1>
<p>登録する商品の情報を入力してください。</p>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@product) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name, "名前" %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.label :current_bid, "価格" %>
      <%= f.text_field :current_bid, class: 'form-control' %>

      <%= f.label :time_left, "取引期間" %>
      <%= f.datetime_field :time_left, class: 'form-control' %>

      <%= f.label :discription, "備考欄" %>
      <%= f.text_area:discription, class: 'form-control' %>

      <%= f.submit "登録する！", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
